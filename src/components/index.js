// this is the entry point for all components
import { HeaderComponent } from "./headers.js";
export {
    HeaderComponent
};
export const componentsLoaded = () => console.log('components have loaded');